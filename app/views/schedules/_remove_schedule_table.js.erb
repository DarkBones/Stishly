
$("#schedule_" + "<%= schedule.hash_id %>").remove();

if ($("table#schedules_<%= remove_from %> tr").length == 1) {
	$("table#schedules_<%= remove_from %>").remove();
	$("#<%= remove_from %>-schedules").append("<p><%= t('schedules.no_' + remove_from + '_schedules') %></p>");
}