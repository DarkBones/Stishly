<%= form_with(model: Account.new) do |f| %>
  <div class="modal-body">
    <div class="container">

      <div class="row py-2">
        <div class="col-12 col-sm-7">
          <%= f.label :name %><br />
          <%= f.text_field :name, placeholder: t('placeholders.current_account'), class: 'form-control' %>
        </div>
        <div class="col-12 col-sm-5">
          <%= f.label :type %><br />
          <%= f.select(:account_type, [[t('account.types.spending'), "spend"], [t('account.types.saving'), "save"]], {selected: "spend"}, {:class => "form-control"}) %>
        </div>
      </div>

      <div class="row py-2">
        <div class="col-12 col-sm-7">
          <%= f.label :balance %><br />
          <%= f.number_field :balance, placeholder: "1000.00", class: 'form-control', step: :any %>
        </div>
        <div class="col-12 col-sm-5">
          <%= f.label :currency %><br />
          <%= f.select(:currency, all_currencies(Money::Currency.table), {selected: current_user.currency}, { :class => 'form-control', "default_option" => current_user.currency }) %>
        </div>
      </div>

      <div class="row py-2">
        <div class="col-12">
          <%= f.label :description %><br />
          <%= f.text_area :description, class: 'form-control' %>
        </div>
      </div>

    </div>
  </div>

  <div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-dismiss="modal"><%= t('buttons.close.text') %></button>
    <%= f.button t('buttons.create_account.text'), :class => 'btn btn-primary', data: {disable_with: image_tag("ajax_spinner_white.svg", height: "20") + " #{t('buttons.create_account.action')} ..."} %>
  </div>
<% end %>