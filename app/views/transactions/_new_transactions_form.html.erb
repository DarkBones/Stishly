<% transaction = Transaction.new.decorate %>
<%= form_with(model: transaction) do |f| %>
  <div class="modal-body">
    <div class="container">

        <%= render partial: "transactions/transaction_form_fields", :locals => {:transaction => transaction, :f => f, :base_only => false, :form_id_prefix => "", :form_id_suffix => "" } %>

    </div>
  </div>

  <div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-dismiss="modal"><%= t('views.buttons.modals.close.text') %></button>
    <%= f.button t('views.transactions.new.create.text'), :class => 'btn btn-primary', data: {disable_with: image_tag("ajax_spinner_white.svg", height: "20") + " #{t('views.transactions.new.create.action')} ..."} %>
  </div>
<% end %>