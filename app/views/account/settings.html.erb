<h3>Account settings</h3>
<%= form_for :user_setting, :url => edit_account_settings_path do |f| %>
  <%= f.select(:currency, all_currencies(Money::Currency.table), selected: Account.get_currency(@account_id, current_user).iso_code) %>

  <%= f.submit 'Save' %>
<% end %>