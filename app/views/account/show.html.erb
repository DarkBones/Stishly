<h2><%= @account_name %> Transactions</h2>
<%= link_to 'Settings', account_settings_path if @account_id != 'all' %>

<% if params[:id] != 'all' %>
  <%= render "new_transaction_form" %>
<% end %>

<%= render "transactions", locals: {transactions: @account_transactions[:transactions]} %>