<%= form_with(model: category, :url => edit_category_path, html: { autocomplete: "off" }) do |f| %>

	<%= render partial: "categories/form_fields", :locals => { :f => f, :category => category } %>

<% end %>

<script>
	$("#category_color").minicolors({
		theme: 'bootstrap'
	});
</script>