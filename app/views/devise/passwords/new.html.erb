<%= content_for :meta_title, t('views.devise.passwords.new.title') %>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>

	<div class="container">

		<div class="row">
			<div class="col-12 py-2">
				<h1><%= t('views.devise.passwords.new.title') %></h1>
			</div>
		</div>

		<div class="row">
			<div class="col-12 py-2">
				<%= f.label :email %>
				<%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
			</div>
		</div>

		<div class="row">
			<div class="col-12 py-2">
				<%= f.button t('views.buttons.forgot_password.text'), :class => 'btn btn-primary', data: {disable_with: image_tag("ajax_spinner_white.svg", height: "20") + " #{t('views.buttons.forgot_password.action')} ..."} %>
			</div>
		</div>

		<div class="row">
			<div class="col-12 py-2">
				<%= render "devise/shared/links" %>
			</div>
		</div>

	</div>

<% end %>