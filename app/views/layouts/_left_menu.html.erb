<% if user_signed_in? && @accounts %>
  <div id="left-menu">
    <h2>Accounts</h2>
    <ul id="accounts_list" data-url="/account/sort">
      <li class="no-sorting">
      <%= form_for :account, :url => url_for(:controller => 'account', :action => 'create') do |f| %>
        <%= f.text_field :name_balance %>
      <% end %>
      </li>
      <% if @accounts.length > 1 %>
        <li class="clickable active no-sorting">
          All accounts <span class="balance">
          <%= @currency_symbol %> <%= balance(@total_balance, @cents_amount, "cents") %></span>
        </li>
      <% end %>
      <% @accounts.each do |a| %>
        <% @balance = 0.to_f %>
        <% if a.balance %>
          <% @balance = a.balance.to_f %>
        <% end %>
        <li class="clickable" id="account_<%= a.id %>">
          <%= a.name %> <span class="balance">
          <%= @currency_symbol %> <%= balance(@balance, @cents_amount, "cents") %></span>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>