<%= content_for :meta_title, t('views.errors.unacceptable.title') %>

<div class="container">
	<div class="row">
		<div class="col-12">
			<h2>
				<%= t('views.errors.unacceptable.title') %>
			</h2>
			<h3>
				<%= t('views.errors.unacceptable.sub_title') %>
			</h3>
		</div>
	</div>
	<div class="row py-2">
		<div class="col-12">
			<p>
				<%= t('views.errors.reason') %>
			</p>
			<ul>
				<li>
					<%= t('views.errors.unacceptable.reasons.one') %>
				</li>
				<li>
					<%= t('views.errors.unacceptable.reasons.two') %>
				</li>
			</ul>
		</div>
	</div>

	<div class="row py-2">
		<div class="col-12">
			<a href="/" role="button" class="btn btn-primary">
				<%= t('views.errors.return') %>
			</a>
		</div>
	</div>
</div>