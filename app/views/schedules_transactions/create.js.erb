<% 
message = t("views.schedules_transactions.confirmation") 
message.sub! "{num}", @number.to_s
message.sub! "{transactions}", @transaction
message.sub! "{schedule}", @schedule
%>


$("[data-dismiss=modal]").trigger({ type: "click" });
triggerNotice('<%= message %>', false);

UpdateDailyBudget();