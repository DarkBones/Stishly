en:
  models:
    account:
      errors:
        name:
          blank: can't be blank
          duplicate: already exists
        plan:
          accounts: :defaults.account.errors.plan.accounts
          spending_accounts: :defaults.account.errors.plan.spending_accounts
      summary:
        name: :defaults.account.summary.name
