en:
  models:
    account:
      errors:
        name:
          blank: can't be blank
          duplicate: already exists
        plan:
          accounts: Upgrade to premium for more accounts
          spending_accounts: Upgrade to premium for more spending accounts
      summary:
        name: :defaults.account.summary.name
