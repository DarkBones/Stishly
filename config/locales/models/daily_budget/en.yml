en:
  models:
    daily_budget:
      statuses:
        infinite:
          messages:
            main: :defaults.daily_budget.statuses.messages.infinite
            sub: :defaults.daily_budget.statuses.messages.invest
        excellent:
          messages:
            main: :defaults.daily_budget.statuses.messages.excellent
            sub: :defaults.daily_budget.statuses.messages.invest
        good:
          messages:
            main: :defaults.daily_budget.statuses.messages.main
            sub: :defaults.daily_budget.statuses.messages.keep_it_up
        fair:
          messages:
            main: :defaults.daily_budget.statuses.messages.main
            sub: :defaults.daily_budget.statuses.messages.caution
        caution:
          messages:
            main: :defaults.daily_budget.statuses.messages.main
            sub: :defaults.daily_budget.statuses.messages.warning
        bad:
          messages:
            main: :defaults.daily_budget.statuses.messages.bad
            sub: :defaults.daily_budget.statuses.messages.danger