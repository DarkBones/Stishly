en:
  models:
    schedule:
      errors:
        period_num:
          negative: "'Run Every' must be greater than zero"
        plan:
          schedule: :defaults.schedule.failures.upgrade
        main:
          duplicate: You can only have one main income schedule
      periods:
        days: :defaults.schedule.periods.days
        weeks: :defaults.schedule.periods.weeks
        months: :defaults.schedule.periods.months
        years: :defaults.schedule.periods.years