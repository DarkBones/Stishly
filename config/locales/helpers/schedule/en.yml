en:
  helpers:
    label:
      schedule:
        name: Name
        schedule: Schedule
        start_date: Start date
        run_every: Run every
        days: Days
        end_date: End date
        exclude: Exclude
        if_exclusion_met: If exclusion met